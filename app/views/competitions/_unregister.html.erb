
<%= form_for(current_user.meetings.find_by(params[:id]),
             html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :attending_competition_id, params[:id] %></div>
  <%= f.submit "Unregister", class: "btn btn-primary" %>
<% end %>
