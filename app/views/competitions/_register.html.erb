<%= form_for(current_user.meetings.
                          build,
                          remote: true) do |f| %>
  <div><%= hidden_field_tag :attending_competition_id, @id %></div>
  <%= f.submit "Register", class: "btn btn-primary" %>
<% end %>


