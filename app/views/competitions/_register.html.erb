<%= form_for(current_user.meetings.
                          build(attending_competition_id: 9),
                          remote: true) do |f| %>
  <div><%= hidden_field_tag :attending_competition_id, 9 %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>

