
	<%= form_for(current_user.meetings.
	                          build(attending_competition_id: params[:id]),
	                          remote: true) do |f| %>
	  <div><%= hidden_field_tag :attending_competition_id, params[:id] %></div>
	  <%= f.submit "Register", class: "btn btn-primary" %>
	<% end %>


